// CSES Counting Problems: All Letter Subgrid Count I

#include <bits/stdc++.h>
using namespace std;

const int MAXN = 3000, B = 11;
vector<vector<vector<int>>> sub(B + 1);

int32_t main() {
    cin.tie(0)->sync_with_stdio(0);

    int n, A;
    cin >> n >> A;
    vector<string> a(n);
    for (auto &x : a) cin >> x;

    for (int i = 0; i <= B; i++) {
    }
}