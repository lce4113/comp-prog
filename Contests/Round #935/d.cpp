// CodeForces Round #935: Problem D

#include <bits/stdc++.h>
using namespace std;

void solve() {
    int m, n;
    cin >> m >> n;
    vector<int> a(n);
    for (auto &x : a) cin >> x;
    vector<int> b(n);
    for (auto &x : b) cin >> x;
    vector<int> dp;
    for (int i = 0; i < n; i++) {
    }
}

int32_t main() {
    int t = 1;
    cin >> t;
    while (t--) solve();
}