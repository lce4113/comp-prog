// Codeforces Round 1051: Problem E

#include <bits/stdc++.h>
using namespace std;

#define int long long

void solve() {
    int n;
    cin >> n;
    for (int i = 0, c = 0; i < n; i++) {
        if (a[i] == ')') {
            if (c == 0) {
                if () }
        }
    }
}

int32_t main() {
    cin.tie(0)->sync_with_stdio(0);

    int t = 1;
    cin >> t;
    while (t--) solve();
}