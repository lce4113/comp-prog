// ––– USACO 2022 January Silver: Problem C –––

#include <bits/stdc++.h>
using namespace std;

using pi = pair<int, int>;
using ll = long long;
#define f first
#define s second

void solve(int a, int b) {
  cout << a << ' ' << b;
  cout << '\n';
}

int32_t main() {
  int t, a, b;
  cin >> t;
  while (t--) {
    cin >> a >> b;
    solve(a, b);
  }
}

/* stuff you should look for
 * int overflow, array bounds
 * special cases (n=1?)
 * do smth instead of nothing and stay organized
 * WRITE STUFF DOWN
 * DON'T GET STUCK ON ONE APPROACH
 */
