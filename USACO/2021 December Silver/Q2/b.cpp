// ––– USACO 2021 December Silver: Problem B –––

#include <bits/stdc++.h>
using namespace std;

using ll = long long;
using pi = pair<ll, ll>;
using db = double;
#define pb push_back
#define lb lower_bound
#define ub upper_bound
#define f first
#define s second

void solve();

int32_t main() {
  int T;
  cin >> T;
  while (T--) {
    solve();
  }
}

void solve() {
  int n, m;
  cin >> n >> m;
  for (int i = 0; i < m; i++) {
    int i, k;
    cin >> i >> k;

  }
}

/* stuff you should look for
 * int overflow, array bounds
 * special cases (n=1?)
 * do smth instead of nothing and stay organized
 * WRITE STUFF DOWN
 * DON'T GET STUCK ON ONE APPROACH
 */
