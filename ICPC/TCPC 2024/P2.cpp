// Problem E

#include <bits/stdc++.h>
using namespace std;

void solve() {
    int n, m, d, x;
    cin >> n >> m >> d >> x;
    vector<pair<int, int>> a(n);
    for (auto &x : a) cin >> x.f >> x.s;
    vector<int> pfs_f(n + 1), pfs_b(n + 1);
    for (int i = 0; i < n; i++) {
    }
}

int32_t main() {
    int t = 1;
    cin >> t;
    while (t--) solve();
}