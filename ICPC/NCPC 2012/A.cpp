#include <bits/stdc++.h>
using namespace std;

#define int long long

void solve() {
    string a, b;
    cin >> a >> b;
    if (a.size() < b.size()) {
        cout << "no\n";
    } else {
        cout << "go\n";
    }
}

int32_t main() {
    int t = 1;
    // cin >> t;
    while (t--) solve();
}